# BOOTSTRAP.md
The user is experiencing "API rate limit reached" on their Google Gemini model in Termux.
They need to switch to Groq (which is free and unblocked) but the interactive menu is confusing.
I will write a script to automatically reconfigure their config file to use Groq if they provide a key, or guide them to get one.
